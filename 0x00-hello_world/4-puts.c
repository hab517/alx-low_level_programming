#include <stdio.h>
/**
 * main-prints the size of various types
 * Return:0 if exited properly,non-zero otherwise
 */
int main(void)
{
puts("\"nprogramming is like building a multilingual puzzle,\n");
return (0);
}
